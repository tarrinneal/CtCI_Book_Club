// In lesson 1, we used a very basic Function to practice setting variables.
// Here we will learn more about them.

// Every language will have it's own syntax for defining and calling functions.
// In general though, functions have Parameters (the values passed into the function),
// Return statements (in many languages, the return type is defined explicitly),
// a name, and a body.

// V key word
// V       V name       V  V parameter names
function addTwoNumbers(a, b) {
  // body

  return a + b; // return statement
}

// In other languages like Dart, Java, Kotlin, Swift, etc
// you'll define types for all inputs and outputs. For example, in Dart
// String combineWords(a String, b String) {}

// Task 1: define a function that takes two numbers and subtracts the second from the first.
// Name it subtraction.

module.exports = subtraction;
