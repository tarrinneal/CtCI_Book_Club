const nextNumber = require('./nextNumber');
const nextNumberSol = require('./nextNumberSol');

test('Basic nextNumber test', () => {
  expect('This test is only to make you look at the console logs, if they match comment this test out')toBe('false')
  expect(true)toBe(true)
});
